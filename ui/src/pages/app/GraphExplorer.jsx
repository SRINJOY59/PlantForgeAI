import { GitBranch } from "lucide-react";
import Placeholder from "../../components/Placeholder";

export default function GraphExplorer() {
  return (
    <Placeholder icon={GitBranch} title="Graph Explorer">
      A force-directed view of the plant — units, the K-301 bridge between them,
      and the causal edges denoise recovered. Wiring to a graph endpoint next.
    </Placeholder>
  );
}
