steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build','-f','infra/docker/service.Dockerfile',
           '--build-arg','SERVICE=${_SERVICE}',
           '-t','${_IMAGE}','.']
images: ['${_IMAGE}']
options: { machineType: 'E2_HIGHCPU_8' }
